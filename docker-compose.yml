version: '3.7'

services:

  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: jargon
    image: jargon
    networks:
      - prod-network
    ports:
      - '80:80'
    environment:
    - PORT=80

networks:
  prod-network:
    driver: bridge
